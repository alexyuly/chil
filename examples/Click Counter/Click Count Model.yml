type:
  component:
    inputs:
      count total: number
      count per click: number
      click:
    output: number

children:
  add: number/add
  replace:
    common/replace:
      state: number

connections:
  count total:
    add: state
    output:
  count per click:
    replace: state
  click:
    replace: action
  replace:
    add: action
  add:
    add: state
    output:

defaults:
  count total: 0
  count per click: 1
