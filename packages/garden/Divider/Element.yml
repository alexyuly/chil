type:
  common/template:
    state: complex
    action:
      complex:
        id: string
        style: string

defaults:
  state:
    type: div
    attributes:
      id:
        prop: id
      style:
        prop: style
