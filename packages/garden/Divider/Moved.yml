type:
  component:
    inputs:
      ids: ../Ids
    output: document/mouse event

children:
  id: Id
  mouse down: document/mouse down
  root id: ../Root/Id
  root mouse move: document/mouse move
  root mouse up: document/mouse up
  enabled: claim/confirm
  disabled: claim/reject
  events:
    common/filter:
      action: document/mouse event

connections:
  ids:
    id: action
    root id: action
  id:
    mouse down: id
  mouse down:
    enabled: action
  root id:
    root mouse move: id
    root mouse up: id
  root mouse move:
    events: action
  root mouse up:
    disabled: action
  enabled:
    events: state
  disabled:
    events: state
  events:
    output:
