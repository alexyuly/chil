type:
  component:
    inputs:
      ids: Ids
      drawer width: number
    output: complex

children:
  divider id: Divider/Id
  divider: Divider/View
  drawer: Drawer/View
  root action:
    complex/combine:
      id: string
      drawer: complex
      divider: complex
  root id: Root/Id
  root: Root/View

connections:
  ids:
    divider id: action
    root id: action
  divider id:
    divider: id
  drawer width:
    drawer: width
    divider: left
  root id:
    root action: id
  drawer:
    root action: drawer
  divider:
    root action: divider
  root action:
    root: action
  root:
    output:
