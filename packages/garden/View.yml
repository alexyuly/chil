type:
  component:
    inputs:
      ids: Ids
      drawer width: number
    output: complex

children:
  divider id: Divider/Id
  divider: Divider/View
  drawer: Drawer/View
  root id: Root/Id
  root action:
    complex/combine:
      id: string
      divider: complex
      drawer: complex
  root: Root/View

connections:
  ids:
    divider id: action
    root id: action
  drawer width:
    divider: left
    drawer: width
  divider id:
    divider: id
  divider:
    root action: divider
  drawer:
    root action: drawer
  root id:
    root action: id
  root action:
    root: action
  root:
    output:
