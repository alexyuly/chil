variables:
  action: complex

type:
  component:
    inputs:
      separator: string
      state: list
      action: action
    output: string

children:
  join: string/join
  template:
    common/template:
      state: list

connections:
  separator:
    join: state
  state:
    template: state
  action:
    template: action
  template:
    join: action
  join:
    output:

defaults:
  separator: ""
