name: counter
of: number
values:
    reset:
        of:
        to: { replace 0: dispatch }
    increment:
        of:
        to: { replace 1: dispatch }
operations:
    replace 0:
        of: { any/abstract/replace: number }
        initial: { set state: 0 }
        to: { index: set state }
    replace 1:
        of: { any/abstract/replace: number }
        initial: { set state: 1 }
        to: { index: dispatch }
    index:
        of: number/add
        to:
            index: set state
            counter:
