name: map
parameters:
    Type:
        any/abstract/pipe:
            Input Type: Input Type
            Output Type: Output Type
is:
    any/abstract/pipe:
        Input Type: { vector: Input Type }
        Output Type: { vector: Output Type }
values:
    dispatch:
        to:
            count: dispatch
            dissect: dispatch
operations:
    count:
        of: vector/count
        to: { collect: set state }
    dissect:
        of: { vector/dissect: { Type: Input Type } }
        to: { operate: dispatch }
    operate:
        of: Type
        to: { collect: dispatch }
    collect:
        of: { vector/collect: { Type: Output Type } }
        to: { map: }
# TODO - account for operators which do not map 1 input to 1 output
